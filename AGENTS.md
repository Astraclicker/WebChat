# WebChat 项目协作约定

## Redis 学习目标

- 本项目是教学练习项目。Redis 被视为位于服务器与 MySQL 之间的内存数据库，用来练习缓存读取、缓存未命中、数据库回源、缓存写入和 TTL。
- 当前阶段优先完成清晰、可运行的小闭环，不按生产系统标准扩展密码哈希、会话令牌、权限控制和分布式一致性设计。
- 登录缓存可以沿用当前项目已有的用户名和明文密码模型；这是教学简化，不代表生产实践。
- 每一步尽量保持改动小并能够独立验证。不要为了接入 Redis 顺带大规模重构其他分支共用的 MySQL 接口。

## 提交节奏

- 只有当一块功能形成完整、可验证的闭环后才提交，不为半成品或占位逻辑创建提交。
- Codex 在建议提交时，同时给出符合 Conventional Commits 的标题以及 `Generated-by: Codex` trailer。
